#include <stdio.h>

struct Item {
    int id;
};

int main() {
    struct Item it[10];
    int n, i;

    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d", &it[i].id);
    }

    printf("Updated List:\n");
    for(i = 0; i < n; i++) {
        printf("%d ", it[i].id);
    }
    return 0;
}
