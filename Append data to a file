#include <stdio.h>

int main() {
    FILE *fp;
    fp = fopen("data.txt", "a");   // append mode

    if (fp == NULL) {
        printf("File not found");
        return 1;
    }

    fprintf(fp, "\nNew data appended");
    fclose(fp);

    printf("Data appended successfully");
    return 0;
}
