#include <stdio.h>
int main() {
    float r[12], sum = 0;
    int i;
    for (i = 0; i < 12; i++) {
        scanf("%f", &r[i]);
        sum += r[i];
    }
    printf("Average = %.2f", sum / 12);
    return 0;
}
