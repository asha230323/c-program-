#include <stdio.h>
int main() {
    int n, p, count = 0, i;
    scanf("%d", &n);
    for (i = 0; i < n; i++) {
        scanf("%d", &p);
        if (p == 1) count++;
    }
    printf("Present = %d", count);
    return 0;
}
