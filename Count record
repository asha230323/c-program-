#include <stdio.h>
int main() {
    FILE *f = fopen("data.txt", "r");
    int count = 0;
    char c;
    while ((c = fgetc(f)) != EOF)
        if (c == '\n') count++;
    fclose(f);
    printf("Records = %d", count);
    return 0;
}
