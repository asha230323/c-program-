#include <stdio.h>
int main() {
    char s[50];
    int i, c = 0;
    gets(s);
    for (i = 0; s[i]; i++)
        if (s[i]=='a'||s[i]=='e'||s[i]=='i'||s[i]=='o'||s[i]=='u')
            c++;
    printf("Vowels = %d", c);
    return 0;
}
