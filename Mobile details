#include <stdio.h>
struct mobile {
    char brand[20];
    int price;
};
int main() {
    struct mobile m;
    scanf("%s %d", m.brand, &m.price);
    printf("%s %d", m.brand, m.price);
    return 0;
}
