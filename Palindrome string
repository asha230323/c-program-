#include <stdio.h>
#include <string.h>
int main() {
    char s[20], r[20];
    gets(s);
    strcpy(r, s);
    strrev(r);
    if (strcmp(s, r) == 0)
        printf("Palindrome");
    else
        printf("Not Palindrome");
    return 0;
}
