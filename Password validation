#include <stdio.h>
#include <string.h>
int main() {
    char p[20];
    scanf("%s", p);
    if (strlen(p) >= 6)
        printf("Valid Password");
    else
        printf("Invalid Password");
    return 0;
}
